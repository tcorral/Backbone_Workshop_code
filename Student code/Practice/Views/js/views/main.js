App.views = App.views || {};
App.views.main = Backbone.View.extend( {
	el: DOMelement, // Add the element being hitched
	events: {},
	addTask: function ( eEvent ) {
		// Show an alert with the content of the input
	}
} );