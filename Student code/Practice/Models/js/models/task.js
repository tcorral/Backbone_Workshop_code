App.models = App.models || {};
App.models.task = Backbone.Model.extend( {
	// El estado por defecto de una tarea es no acabada.(finished)
	// Valida que la descripci�n no est� vacia. (description)
} );